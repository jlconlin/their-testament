# Dependencies / build
node_modules/
dist/

# Generated output — regenerable, and embeds personal annotation text
out/
*.pdf

# Personal data / fetched content — do not commit
data/raw/
data/cache/
.env
secrets/
*.token

# Web — local test scaffolding (but NOT _headers / _redirects, which deploy)
web/_*.html

# Python (legacy)
__pycache__/
*.pyc
.venv/

# LaTeX aux (legacy)
*.aux
*.log
*.fls
*.fdb_latexmk

# Local-only working docs (aggregate stats on real corpus)
docs/private/
